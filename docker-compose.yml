version: "3.9"

services:
  holiday-bot:
    build: .
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - SEND_HOUR=9
      - SEND_MINUTE=0
    volumes:
      - ./data:/app/data
    working_dir: /app
